<style scoped>
    a,
    a:link,
    a:visited,
    a:hover,
    a:active {
      color: #000000;
      text-decoration: none;
      font-weight: normal;
      display: block;
    }
    body {
        margin: 0;
        padding: 0;
    }
    .border-box {
        -webkit-box-sizing: border-box;
        -moz-box-sizing: border-box;
        box-sizing: border-box;
    }
    .sdk-bg {
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
    }
    .sdk-con {
        position: absolute;
        width: 290px;
        height: 50px;
        font-family: 'open sans', arial, sans-serif, 'Helvetica Neue', Helvetica, sans-serif;
        z-index: 1;
    }
    .sdk-bg-color {
        background: #EEEFEF;
    }
    .flex-con {
        display: -webkit-flex;
        display: -moz-flex;
        display: -ms-flexbox;
        display: -ms-flex;
        display: flex;
    }
    .flex-left {
        -webkit-flex: 0 0 50px;
        -moz-flex: 0 0 50px;
        -ms-flex: 0 0 50px;
        flex: 0 0 50px;
    }
    .flex-left img {
        height: 50px;
        width: auto;
    }
    .flex-middle {
        -webkit-flex: 0 0 150px;
        -moz-flex: 0 0 150px;
        -ms-flex: 0 0 150px;
        flex: 0 0 150px;
        padding: 2px 2px;
    }
    .flex-middle .sdk-title {
        border: none;
        text-align: left;
        color: #CE4500;
    }
    .flex-middle .sdk-desc {
        max-height: 25px;
        margin-top: 3px;
        line-height: 12px;
        font-size: 10px;
        color: #AEACA9;
        overflow: hidden;
    }
    .flex-right {
        -webkit-flex: 0 0 90px;
        -moz-flex: 0 0 90px;
        -ms-flex: 0 0 90px;
        flex: 0 0 90px;
        padding: 15px 5px;
    }
    .flex-right .sdk-cta {
        height: 15px;
        padding: 2px;
        font-size: 12px;
        color: #ffffff;
        background: #B3C833;
        line-height: 15px;
        text-align: center;
    }
    .sdk-ad {
         position: absolute;
         top: 0px;
         left: 235px;
         font-size: 8px;
         color: #AEACA9;
    }
</style>

<div class="sdk-bg sdk-bg-color"></div>
<a target="_top" href="{{{data.click_url}}}">
    <div class="sdk-con">
        <div class="flex-con">
            <div class="flex-left">
                <img src={{{data.icon}}} />
            </div>
            <div class="flex-middle border-box">
                <div class="sdk-title">{{{data.title}}}</div>
                <div class="sdk-desc">{{{data.desc}}}</div>
            </div>
            <div class="flex-right border-box">
                <div class="sdk-cta">{{{data.cta}}}</div>
            </div>
            <div class="sdk-ad">(sponsored ad)</div>
        </div>
    </div>
    {{#data.trackers}}
    <img style="display:none;" src={{{url}}} />
    {{/data.trackers}}
</a>